package org.geetha.testng;

import java.util.*;

public class CodingExercise {
	
	private
		static Integer arrayA[] = {1,2,3,4,10,20,30};
		static Integer arrayB[] = {10,20,1,3,4,5};
		
	public void addMap(){
		HashMap<Integer, Integer> m1 = new HashMap<Integer, Integer>();
		
			for(int count : arrayA) {
				if(m1.get(count) != null){
					int num = m1.get(count);
					m1.put(count, ++num);
				} else {
					m1.put(count, 1);
				}
			}
			for(int count : arrayB) {
				if(m1.get(count) != null){
					int num = m1.get(count);
					m1.put(count, ++num);
				} else {
					m1.put(count, 1);
				}
			}

		Iterator<HashMap.Entry<Integer, Integer>> print = m1.entrySet().iterator();
		while(print.hasNext()) {
			HashMap.Entry<Integer, Integer> pair1 = print.next();
			System.out.println(pair1.getKey() + ":" + pair1.getValue());
		}
	}
}


